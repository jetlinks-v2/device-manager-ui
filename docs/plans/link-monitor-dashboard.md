# 运维仪表盘多节点监控优化

## 当前结论与目标

`views/link/DashBoard` 继续作为只读监控感知页，优化目标是让运维人员在多个服务节点下快速判断 CPU、内存与网络资源变化，并减少全部节点折线重叠造成的辨识困难。

本次计划：

1. CPU 趋势支持“JVM 使用率 / 系统使用率”切换，默认展示 JVM；内存趋势同样默认 JVM。
2. 内存趋势支持“JVM 内存 / 系统内存”切换，曲线展示使用率，Tooltip 与节点摘要同时展示“已用 / 总量”。
3. 多节点采用“全节点概览 + 默认聚焦少量高使用节点 + 搜索多选”的渐进式展示；同一节点在各图表中保持稳定颜色。
4. 顶部四个单节点仪表卡替换为紧凑的多节点实时资源排行：按“节点 / 系统 / JVM / 磁盘 / 网络”分组展示。系统组包含 CPU 使用率、1 / 5 / 15 分钟负载、逻辑核心数和内存；CPU 标签保持固定，CPU 次级说明使用“1 分钟负载 / 逻辑核心数”展示归一化负载压力、核心数和状态，悬浮时展示 1 / 5 / 15 分钟原始值及逐项换算结果。核心数为 0 或缺失时退回原始负载值，不伪造压力百分比。JVM 组使用 CPU、内存两行；内存进度表示堆内存使用率，次级信息同时展示堆内存已用 / 总量和堆外已用量。堆外内存不单独绘制使用率，也不参与 JVM 压力排序。系统 / JVM 组统一使用“CPU / 内存”短标签和对齐的进度 / 百分比列。磁盘展示使用率、容量及同一行的“读 / 写”吞吐；网络依次展示同一行的“下行 / 上行”吞吐、占据左半格并与上下行对齐的连接数、同一行的“丢包 / 重传”比率。实时表使用约 5 行的固定可视高度，全部节点在表内纵向滚动，表头和节点列固定；不再通过展开 / 收起改变页面高度。
   实时采样间隔为 3 秒，首帧确定默认顺序后保持稳定；后续采样和刷新不重新排列，只有用户点击列头才改变排序。
   实时节点行只保留本地背景 Hover，不再联动下方历史图，避免临时系列或加粗状态残留。
   节点名称颜色点、节点聚焦选项和三张历史曲线统一使用基于节点名称哈希的稳定颜色映射，历史折线显式设置颜色，不依赖 ECharts 系列顺序。实时表列宽调整为节点 10.5rem、系统 / JVM 各 18.5rem、磁盘 12.5rem、网络 14rem；系统 / JVM 内部的 `CPU / 内存` 标签使用 3rem 右对齐轨道，与进度条保持 0.375rem 间距，把剩余宽度交给进度条。磁盘读写按内容宽度分配并保持两端对齐，避免长写入速率压缩标签；磁盘与网络宽度重新分配后整表总宽不变。
5. 每个卡片右上角增加独立刷新按钮：默认 / 快捷时间推进到最新，自定义时间保持原区间后重新查询。
6. CPU、内存、网络分别维护节点聚焦状态和本地缓存；共享默认重点节点算法，但任一处修改只让当前图表立即重绘。当前会话允许用户主动清空选择，重新进入页面时空缓存不作为有效初始态，而是恢复按资源压力选出的默认重点节点，保证趋势首次展示至少有一个聚焦节点。实时节点集合发生变化时三张历史卡片各自刷新一次可选节点，普通三秒采样不重复请求历史数据。
7. 网络趋势同时展示上行与下行，并移动到 CPU / 内存趋势下方；不修改现有查询契约。
8. 网络趋势保持后端时间桶内的周期流量口径；Tooltip 按节点合并上下行到同一行，并按节点较高方向的周期流量降序展示。
9. 实时磁盘补充读 / 写吞吐，实时网络补充上 / 下行吞吐；两者基于相邻 websocket 快照的累计 counter 与采样时间计算 `B/s`。首帧、能力不可用、时间未推进或 counter 回退时展示 `--`。底部网络历史图仍保持周期流量口径，不与实时速率混用。
10. 实时表格 Hover 背景直接作用于整行全部单元格，固定节点列与普通列复用相同背景基线及过渡，避免视觉延迟。网络历史响应返回后校验当前聚焦节点：保留仍存在的选择；当前选择为空或节点已全部失效时，优先恢复当前历史数据中可用的默认节点，再按最新周期流量选取重点节点，保证历史数据存在时至少有一个可见系列。
11. 节点健康度继续沿用紧凑监控感知表，不新增 KPI 墙或常驻诊断列。在现有 CPU / 内存 / 磁盘 / 网络主信息旁增加按需健康信号：CPU 优先显示 IO 等待、虚拟化 steal、cgroup 限流、温度 / 热降频；磁盘优先显示 inode、IO 队列 / 利用率、块设备异常 / IO 错误与 SMART/NVMe 健康；网络优先显示接口错误、TCP 失败 / 重置、队列积压、softnet / listen / backlog 丢弃、socket 和 conntrack 容量。正常行保持当前密度，只有异常时在节点列追加健康摘要，详细证据通过 Tooltip 展示；指标不可用不等同异常。

## 影响范围与 Owning Module

- Owning module：`runtime-ui/modules/device-manager-ui`
- 页面入口：`views/link/DashBoard/index.vue`
- 页面组件：`views/link/DashBoard/components/`
- 历史数据请求：`api/link/dashboard.ts`
- 用户可见文案：`locales/lang/zh.json`、`locales/lang/en.json`

现有实时 websocket 已返回最新 `SystemInfo` 的 `cpu`、`memory`、`disk`、`network` 快照；CPU 包含使用率、1 / 5 / 15 分钟负载与逻辑核心数，网络包含当前 TCP 连接 gauge 与累计 counter。本次不新增后端字段或接口，丢包率、重传率及新增 counter 型健康信号由同一节点相邻快照的有效差值计算；只在对应 `*Available` 为真时参与健康判断。

## 明确不做

- 不修改 `ui/`、`runtime-ui/modules/jetlinks-components-ui` 或后端监控采集实现。
- 不新增阈值配置、告警处置、节点详情和跨页面下钻。
- 不重构全局导航与页面壳层，不引入经营分析式 KPI 数字墙。

## 实施入口

1. 提取历史监控值归一化、内存容量换算、稳定节点颜色和默认聚焦规则。
2. 用现有实时 websocket 构建紧凑的多节点资源排行，统一归一化系统 CPU / 负载 / 核心数、JVM CPU、系统 / JVM 内存、磁盘和网络诊断摘要；首帧、计数器回退、指标不可用或无有效分母时网络比率展示 `--`。分组列头负责排序，列表负责前 5 折叠与全部展开，替换 `TopCard`。
3. 收敛 CPU / 内存趋势组件，使指标切换保持时间范围和节点选择状态，并默认 JVM。
4. 将横向滚动节点图例升级为可搜索、多选、恢复默认的节点聚焦控件。
5. 为每张卡片提供独立刷新入口；区分快捷时间和自定义时间。
6. 页面分别管理 CPU / 内存 / 网络节点选择；三者共享默认重点节点算法，但后续选择互不影响。网络一次查询上行和下行两组数据并在底部展示。
7. 补齐中英文 i18n，清理调试输出和无效状态。
8. 扩展统一实时快照状态机，计算磁盘读写与网络收发吞吐；磁盘、网络仍各占一个一级列，通过两层紧凑摘要展示，避免新增横向指标列。

## 验证与风险

- 已验证：单节点、多节点、无数据和部分字段缺失的归一化测试；指标切换状态、独立节点聚焦、稳定颜色、紧凑列宽与横向滚动关联。
- 已执行：`device-manager-ui` 测试、生产构建、locale JSON、差异格式与 Vue 文件行数门禁，以及 `/iot/link/dashboard` 浏览器交互复查。
- 剩余风险：实时吞吐数值在极端大单位或超长本地化文案下仍依赖紧凑格式；后续新增网络指标应进入诊断下钻，不继续挤入当前三级摘要。
- 验证结果：`pnpm --filter device-manager-ui test:agent-tools` 40/40 通过；`pnpm --filter jetlinks-web-core build -- --module-name device-manager-ui` 通过；locale JSON、`git diff --check`、Vue 文件 300 行门禁通过。浏览器确认实时资源按节点 / 系统 / JVM / 磁盘 / 网络五组展示，系统与 JVM 均使用“CPU / 内存”标签并对齐，1 / 5 / 15 分钟负载作为系统 CPU 次级说明，逻辑核心数为 0 时不展示；JVM 堆内存 / 堆外内存在同一条次级说明展示。列表固定约 5 行高度，超出节点在表内滚动，表头与节点列保持固定，分组列头可排序。当前节点 / 系统 / JVM / 磁盘 / 网络列宽实测分别为 168 / 296 / 296 / 200 / 224px；磁盘“读 / 写”、网络“下行 / 上行”和“丢包 / 重传”均无横向溢出并保持约 17.5px 单行高度，列表行高仍约 70.3px。固定节点列、磁盘列与网络列连续衔接，节点列不遮挡磁盘标签。网络首帧或无有效差值时丢包率 / 重传率显示 `--`；网络 Tooltip 按节点将上下行周期流量合并为一行，不追加速率单位。CPU、内存和网络分别使用独立受控状态与本地缓存；实时节点集合仅在节点加入或离开时触发一次历史数据刷新，普通三秒采样不重复请求。健康状态机覆盖可用性门控、首帧、时间 / counter 回退、同采集时间保持和节点离开清理；当前健康环境无异常标签且正常行高保持 70.3px，浏览器控制台无错误。构建仅保留仓库既有 warning。
- CPU 负载压力补充验证：`pnpm --filter device-manager-ui test:agent-tools` 33/33 通过，覆盖核心数缺失、70% / 100% 状态边界及超过 100% 的过载值；模块构建与中英文 locale JSON 校验通过。浏览器实测显示“负载 15.0% · 18核 · 正常”，Tooltip 展示 1 / 5 / 15 分钟原始值和逐项按核心换算结果；系统单元格 `clientWidth / scrollWidth` 均为 296px，列表行高约 70.3px，未因状态展示产生溢出或明显增高。
- 系统 / JVM 指标间距补充验证：标签轨道由 88px 收敛为 48px 并右对齐，标签到进度条、进度条到百分比均为 6px；进度条由 126px 增长到 166px。两列单元格仍为 296px，`clientWidth / scrollWidth` 和 `clientHeight / scrollHeight` 一致，列表行高保持约 70.3px；生产构建通过。
- TCP 连接数对齐补充验证：通用指标组新增单值紧凑模式，连接数占据网络列左半格，标签左边缘和数值右边缘分别与“下行 / 丢包”对齐；网络单元格 `clientWidth / scrollWidth` 均为 240px，列表行高保持约 70.3px。`test:agent-tools` 33/33、生产构建、差异格式和 Vue 文件 300 行门禁通过。
- 磁盘 / 网络宽度补充验证：磁盘由 11.5rem 增至 12.5rem（200px），网络由 15rem 收敛到 14rem（224px），整表总宽保持 74rem。磁盘读写使用通用按内容分配模式，两个标签保持完整并分别贴合各自数值，读指标左对齐、写指标右对齐；磁盘 / 网络单元格 `clientWidth / scrollWidth` 一致，列表行高保持约 70.3px。`test:agent-tools` 33/33、生产构建、差异格式和 Vue 文件 300 行门禁通过。
- 节点健康补充验证：健康状态采用集中阈值和可解释证据，不把 TCP 连接数等无容量基线的 gauge 直接判为异常。CPU、磁盘、网络所有可选诊断均先检查对应 `*Available`；counter 只使用有效相邻快照，时间 / counter 回退和能力变为不可用会清除旧证据，同一采集时间保持上次有效结论。CPU 临界温度不可用时，Tooltip 展示实际采用的默认参考阈值，不渲染 `临界 --`；后端提供临界温度时则展示按其比例计算的参考阈值。正常节点不渲染摘要，当前 5 行浏览器实测均为约 70.3px；异常测试覆盖 IO wait、磁盘忙碌、conntrack 容量、cgroup 限流、温度阈值、块设备和 SMART 失败。
- Git commit / Pull Request：pending
