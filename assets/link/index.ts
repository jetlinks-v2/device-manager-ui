import certificate from '../certificate.png'
import jar from '../jar.png'
import plug from '../plug.png'
import plugin from '../plug.png'
import local from '../local.png'
import protocol from '../protocol.png'
import network from '../network.png'
import marketplace from '../plug.png'

export const link = {
    certificate,
    jar,
    plug,
    plugin,
    local,
    protocol,
    network,
    /** 能力市场（暂复用 plug 图标） */
    marketplace,
}
