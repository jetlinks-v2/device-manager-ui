/**
 * Target cloud module does not mount the Nuxt prototype dev-tools panel.
 * Keep this composable as a no-op so migrated pages can retain the same lifecycle boundary.
 */
export function useIotMockDevTools(): void {}

